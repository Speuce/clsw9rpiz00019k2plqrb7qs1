{
    "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
    "installCommand": "(cd client && npm i) && (cd api && npm i)",
    "buildCommand": "cd client && npm run build",
    "outputDirectory": "client/build"
}